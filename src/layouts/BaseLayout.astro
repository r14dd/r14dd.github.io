<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Riad Mukhtarov</title>

    <link rel="icon" href="/favicon.ico" />


    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=JetBrains+Mono:wght@400;500&display=swap"
      rel="stylesheet"
    />

    <style is:global>
      :root {
        --bg: #050505;
        --text: #eeeeee;
        --text-soft: #a1a1a1;
        --muted: #888888;
        --accent: #dcdcdc;
        --border: rgba(255, 255, 255, 0.06);
      }

      * {
        box-sizing: border-box;
      }

      html {
        background: var(--bg);
        scroll-behavior: smooth;
      }

      body {
        background: var(--bg);
        color: var(--text-soft);
        font-family: "Inter", system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
        margin: 0;
        padding: 5.5rem 1.5rem 4rem;
        line-height: 1.8;
        font-size: 16px;
      }

      .content {
        max-width: 840px;
        margin: 0 auto;
      }

      h1 {
        font-size: 2.6rem;
        margin: 0 0 0.4rem;
        font-weight: 600;
        letter-spacing: -0.01em;
        color: var(--text);
      }

      h2 {
        font-size: 1.45rem;
        margin: 0 0 1rem 0;
        font-weight: 600;
        letter-spacing: 0.02em;
        color: var(--text);
      }

      h3 {
        font-size: 1rem;
        margin: 0.7rem 0 0.4rem 0;
        font-weight: 500;
        color: var(--text);
      }

      section {
        padding: 0;
        margin: 3rem 0 0;
      }

      p {
        margin-top: 0.6rem;
        color: var(--text-soft);
      }

      small {
        display: block;
        margin-bottom: 0.6rem;
        color: var(--muted);
      }

      ul {
        padding-left: 1.2rem;
        margin-top: 0.6rem;
      }

      li {
        margin-bottom: 0.4rem;
        color: var(--text-soft);
      }

      li::marker {
        color: var(--muted);
      }

      code {
        background: rgba(255, 255, 255, 0.06);
        border: 1px solid rgba(255, 255, 255, 0.1);
        padding: 0.2rem 0.4rem;
        border-radius: 4px;
        color: var(--text);
        font-size: 0.88em;
        font-family: "JetBrains Mono", ui-monospace, SFMono-Regular, Menlo, monospace;
      }

      strong {
        color: var(--text);
      }

      em {
        color: var(--muted);
        font-style: italic;
      }

      a {
        color: var(--text);
        text-decoration: none;
        border-bottom: 1px solid rgba(237, 237, 237, 0.25);
        transition: opacity 0.2s ease, border-color 0.2s ease;
      }

      a:hover {
        opacity: 0.85;
        border-color: rgba(237, 237, 237, 0.7);
      }

      ::selection {
        background: rgba(237, 237, 237, 0.2);
        color: var(--text);
      }

      footer {
        margin-top: 4rem;
        padding-top: 2rem;
        text-align: center;
        font-size: 0.75rem;
        color: var(--muted);
        border-top: 1px solid var(--border);
      }

      .section-block {
        padding-top: 1rem;
        border-top: 1px solid var(--border);
        margin-top: 3.2rem;
      }

      .side-nav {
        position: fixed;
        left: 2.5rem;
        top: 50%;
        transform: translateY(-50%);
        display: flex;
        flex-direction: column;
        gap: 0.7rem;
        padding: 0.8rem 1rem;
        background: rgba(5, 5, 5, 0.6);
        border: 1px solid rgba(255, 255, 255, 0.06);
        border-radius: 12px;
        backdrop-filter: blur(10px);
        z-index: 10;
      }

      .side-nav a {
        border-bottom: none;
        color: var(--text-soft);
        font-size: 0.85rem;
        transition: color 0.2s ease, opacity 0.2s ease;
      }

      .side-nav a.active {
        color: var(--text);
      }

      .side-nav.blurred {
        background: rgba(5, 5, 5, 0.75);
        backdrop-filter: blur(14px);
      }

      .side-links {
        position: fixed;
        right: 2.5rem;
        top: 50%;
        transform: translateY(-50%);
        display: flex;
        flex-direction: column;
        gap: 0.7rem;
        padding: 0.8rem 1rem;
        background: rgba(5, 5, 5, 0.6);
        border: 1px solid rgba(255, 255, 255, 0.06);
        border-radius: 12px;
        backdrop-filter: blur(10px);
        z-index: 10;
      }

      .side-links a {
        border-bottom: none;
        color: var(--text-soft);
        font-size: 0.85rem;
        transition: color 0.2s ease, opacity 0.2s ease;
      }

      .side-links.blurred {
        background: rgba(5, 5, 5, 0.75);
        backdrop-filter: blur(14px);
      }

      .side-links .mono {
        font-size: 0.7rem;
        letter-spacing: 0.12em;
        text-transform: uppercase;
        color: var(--muted);
      }

      .mobile-bar {
        display: none;
        position: fixed;
        left: 50%;
        transform: translateX(-50%);
        bottom: calc(1rem + env(safe-area-inset-bottom));
        z-index: 12;
        background: rgba(10, 10, 10, 0.7);
        border: 1px solid rgba(255, 255, 255, 0.08);
        border-radius: 999px;
        padding: 0.4rem 0.8rem;
        display: flex;
        align-items: center;
        gap: 0.6rem;
        backdrop-filter: blur(8px);
      }

      .hamburger {
        width: 36px;
        height: 32px;
        border: 1px solid rgba(255, 255, 255, 0.1);
        background: rgba(10, 10, 10, 0.6);
        border-radius: 8px;
        display: flex;
        flex-direction: column;
        gap: 4px;
        align-items: center;
        justify-content: center;
        padding: 0;
      }

      .hamburger span {
        display: block;
        width: 18px;
        height: 2px;
        background: var(--text);
        opacity: 0.7;
      }

      .mobile-current {
        font-size: 0.85rem;
        color: var(--text-soft);
        letter-spacing: 0.02em;
        white-space: nowrap;
      }

      .mobile-current.active {
        color: var(--text);
        text-shadow: 0 0 10px rgba(237, 237, 237, 0.35);
      }

      .mobile-menu {
        display: none;
        position: fixed;
        left: 1rem;
        right: 1rem;
        bottom: calc(4.2rem + env(safe-area-inset-bottom));
        background: rgba(10, 10, 10, 0.8);
        border: 1px solid rgba(255, 255, 255, 0.08);
        border-radius: 10px;
        padding: 0.8rem 1rem;
        backdrop-filter: blur(10px);
        z-index: 12;
      }

      .mobile-menu.open {
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
        text-align: center;
      }

      .scroll-rail {
        position: fixed;
        left: 1.2rem;
        top: 0;
        bottom: 0;
        width: 1px;
        background: rgba(255, 255, 255, 0.08);
        z-index: 9;
      }

      .scroll-progress {
        position: absolute;
        left: 0;
        top: 0;
        width: 1px;
        height: 0;
        background: rgba(255, 255, 255, 0.35);
        transition: height 0.15s ease;
      }

      .mono {
        font-family: "JetBrains Mono", ui-monospace, SFMono-Regular, Menlo, monospace;
        font-size: 0.85rem;
        letter-spacing: 0.01em;
        color: var(--muted);
      }
      .reading-list li {
        display: grid;
        grid-template-columns: minmax(180px, 1fr) minmax(260px, 2fr);
        gap: 1rem 1.5rem;
        align-items: baseline;
        white-space: normal;
      }

      .reading-quote {
        color: var(--muted);
        font-size: 0.85rem;
        text-align: left;
      }

      .reading-inline {
        margin-top: 0.8rem;
        display: flex;
        flex-direction: column;
        gap: 0.6rem;
      }

      .project-meta {
        display: flex;
        flex-wrap: wrap;
        gap: 0.6rem 1rem;
        align-items: baseline;
      }

      .experience-header {
        display: flex;
        align-items: baseline;
        justify-content: space-between;
        gap: 1rem;
        flex-wrap: wrap;
      }

      .experience-subheader {
        display: flex;
        align-items: baseline;
        justify-content: space-between;
        gap: 1rem;
        flex-wrap: wrap;
      }

      .experience-stack {
        display: flex;
        flex-direction: column;
        gap: 0.2rem;
        margin-bottom: 0.4rem;
      }

      .company-name {
        color: var(--text);
        font-weight: 500;
        font-size: 0.95rem;
      }

      .exp-meta {
        opacity: 0.4;
        transition: opacity 0.2s ease;
        font-size: 0.85rem;
        font-weight: 400;
        font-family: inherit;
        line-height: 1.4;
        color: var(--muted);
      }

      .project-card:hover .exp-meta {
        opacity: 1;
      }

      .meta-block {
        display: flex;
        flex-direction: column;
        gap: 0.2rem;
      }


      .job-title {
        font-weight: 600;
        color: var(--text);
      }

      .project-tech {
        color: var(--muted);
        font-size: 0.85rem;
        font-weight: 400;
        line-height: 1.4;
        opacity: 0.4;
        transition: opacity 0.2s ease;
      }

      .tech-meta {
        color: var(--muted);
        font-size: 0.85rem;
        font-weight: 400;
        line-height: 1.4;
        opacity: 0.4;
      }

      .project-header {
        display: flex;
        align-items: baseline;
        justify-content: space-between;
        gap: 1rem;
        flex-wrap: wrap;
      }

      .project-link {
        opacity: 0;
        transition: opacity 0.2s ease, border-color 0.2s ease;
        border: 1px solid rgba(255, 255, 255, 0.12);
        padding: 0.3rem 0.8rem;
        border-radius: 999px;
        font-size: 0.8rem;
        color: var(--text-soft);
        text-decoration: none;
      }

      .project-card:hover .project-link {
        opacity: 1;
      }

      .project-card:hover .project-link:hover {
        border-color: rgba(255, 255, 255, 0.5);
      }

      .project-card:hover .project-tech {
        opacity: 1;
      }

      .simulate-btn {
        background: transparent;
        border: 1px solid rgba(255, 255, 255, 0.12);
        color: var(--text-soft);
        padding: 0.3rem 0.8rem;
        border-radius: 999px;
        font-size: 0.8rem;
        cursor: pointer;
        transition: opacity 0.2s ease, border-color 0.2s ease;
      }

      .project-actions .simulate-btn {
        opacity: 0;
      }

      .project-card:hover .simulate-btn {
        opacity: 1;
      }

      .project-card:hover .simulate-btn:hover {
        border-color: rgba(255, 255, 255, 0.5);
      }

      .sim-visual {
        margin-top: 0.6rem;
        opacity: 0;
        max-height: 0;
        overflow: hidden;
        transition: opacity 0.2s ease, max-height 0.2s ease;
      }

      .sim-visual.active {
        opacity: 1;
        max-height: 120px;
      }

      .sim-node {
        fill: rgba(255, 255, 255, 0.15);
        stroke: rgba(255, 255, 255, 0.2);
      }

      .sim-link {
        stroke: rgba(255, 255, 255, 0.3);
        stroke-width: 1;
      }

      .sim-pulse {
        fill: rgba(255, 255, 255, 0.7);
      }

      .sim-visual.active .sim-pulse {
        animation: pulse 1.8s ease-in-out infinite;
      }

      .sim-visual.active .sim-link {
        stroke-dasharray: 3 4;
        animation: dash 1.2s linear infinite;
      }

      @keyframes pulse {
        0%, 100% { opacity: 0.4; transform: scale(0.9); }
        50% { opacity: 1; transform: scale(1.2); }
      }

      @keyframes dash {
        to { stroke-dashoffset: -14; }
      }

      .skills-line {
        white-space: nowrap;
      }

      .content ul li {
        white-space: nowrap;
      }

      .reading-list li {
        white-space: normal;
      }

      .reading-inline {
        margin-top: 0.3rem;
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
      }

      .reading-row {
        display: grid;
        grid-template-columns: minmax(200px, 1fr) minmax(280px, 2fr);
        gap: 1rem 1.5rem;
        align-items: baseline;
      }

      .author {
        color: var(--text);
        font-weight: 500;
      }

      .project-actions {
        display: flex;
        gap: 0.6rem;
        align-items: center;
        justify-content: flex-end;
        min-width: 140px;
      }

      .hero-links {
        display: none;
        gap: 1rem;
        margin-top: 0.6rem;
        flex-wrap: wrap;
      }


      @media (max-width: 1169px) {
        body {
          padding: 3rem 1rem 3rem;
        }

        .side-nav {
          display: none;
        }

        .side-links {
          display: none;
        }

        .scroll-rail {
          display: none;
        }

        .mobile-bar {
          display: flex;
          bottom: calc(2.2rem + env(safe-area-inset-bottom));
        }

        .mobile-menu {
          bottom: calc(5.4rem + env(safe-area-inset-bottom));
        }

        .hero-links {
          display: flex;
        }

        .experience-header,
        .experience-subheader {
          flex-direction: column;
          align-items: flex-start;
        }

        .project-header {
          flex-direction: column;
          align-items: flex-start;
        }

        .project-actions {
          justify-content: flex-start;
          min-width: unset;
        }

        .project-card .project-link,
        .project-card .simulate-btn {
          opacity: 1;
        }

        .project-tech {
          opacity: 0.6;
        }

        .tech-meta {
          opacity: 0.6;
        }

        .exp-meta {
          opacity: 0.6;
        }

        .project-card:hover .exp-meta {
          opacity: 0.7;
        }

        .reading-quote {
          max-width: 100%;
          text-align: left;
        }

        .content ul li {
          white-space: normal;
        }

        .skills-line {
          white-space: normal;
        }

        .reading-list li {
          grid-template-columns: 1fr;
        }

        .reading-row {
          grid-template-columns: 1fr;
          gap: 0.25rem;
        }

        .author {
          color: var(--text);
        }

        .reading-quote {
          color: var(--muted);
        }
      }

      @media (min-width: 1170px) {
        .mobile-bar,
        .mobile-menu {
          display: none !important;
        }
      }

    </style>
  </head>

  <body>
    <slot />

    <footer>
  © 2026 Riad Mukhtarov. All rights reserved.
</footer>
  </body>
</html>
