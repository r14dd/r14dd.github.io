{
  "name": "analytics-api",
  "private": true,
  "description": "Analytics proxy for the riad.cc /admin dashboard — Cloudflare Worker over the GraphQL RUM API",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.105.0"
  }
}
